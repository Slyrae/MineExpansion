package net.slyrae.mineexpansion.util.handlers;

import net.minecraft.client.renderer.block.model.ModelResourceLocation;
import net.minecraft.init.Items;
import net.minecraft.item.Item;
import net.minecraft.util.ResourceLocation;
import net.minecraftforge.client.model.ModelLoader;
import net.slyrae.mineexpansion.Config;

public class ReplaceTextureHandler {

	/*public static void replaceItemTextures()
	{
		boolean enabled = Config.replaceTextures;
		if(enabled) {
			Items.IRON_INGOT.setRegistryName(setResourceLocation(Items.IRON_INGOT));
		}
	}
	
	private static ResourceLocation setResourceLocation(Item item) {
		ModelLoader.setCustomModelResourceLocation(item, 0, new ModelResourceLocation(item.getRegistryName(), "inventory"));
		return item.getRegistryName();
	}*/
}
